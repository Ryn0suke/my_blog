export type NotionProperties = {
    id: string
    name: string
    createdate: string
    isPrivate: boolean,
    title: string,
    types: string[]
};

export type NotionTag = {
    id: string
    name: string
    color: string
};
